# NO CAP — Environment Variables
# The browser must NEVER contain private AI credentials.
# Set these on your local environment (.env.local) or in Vercel Project Settings -> Environment Variables.

# NVIDIA NIM API Key (Server-Side Only)
# Get your API key from NVIDIA Build: https://build.nvidia.com/
NVIDIA_API_KEY=your_nvidia_api_key_here
